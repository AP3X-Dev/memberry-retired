node_modules/
dist/
.env
*.tsbuildinfo

# MemBerry data (user-specific memory exports). Never force-add these paths to a
# source repository. Legacy .amp/ remains ignored for upgraded installations.
.memberry/
.amp/

# Compiled wiki output (generated, not source)
/wiki/

# Graph analysis/export artifacts (generated, not source)
memberry-graph-out/

# Claude Code runtime state (logs, trackers, etc.)
.claude/
# But keep the distributable skills tracked
!.claude/skills/

# === Planning / design / PRP docs (internal — never published) ===
# All planning, design, and build-prep material lives in docs/ and must not ship.
# Agent instructions and skills are NOT under docs/ — they live in skills/,
# .claude/skills/, .codex/, .augment/, *.example, .cursorrules — and stay tracked.
docs/
# PRPs and planning/handoff notes, wherever they live
PRPs/
*.prp.md
prp-*.md
*handoff*.md
*-handoff-*.md
*.plan.md
amp-three-tier-architecture.md
.superpowers/

# === Optimizer-loop working docs (internal bookkeeping — never published) ===
# The self-hosted hardening loop's state, scaffold, driver, and seed audit. Kept
# locally for resume but not shipped. (docs/ above already covers the driver +
# audit; these two are outside docs/.)
agent-state/
AGENTS.md

# Temp and data files
*.csv
*.csv#
.~lock.*

# Unused workspace
packages/oni/

# Personal agent configs (use .example templates instead)
CLAUDE.md
GEMINI.md

# === Universal ===
.DS_Store
Thumbs.db
__pycache__/
.venv/
*.log
*.tmp
*.swp

# === Secrets / keys ===
*.pem
*.key
id_rsa*
credentials.json
serviceAccountKey.json
*.p12
*.pfx

# === Environment files (keep the example template) ===
.env.*
!.env.example

# Internal planning doc (PRP) — never ships; preserved in git history
SETUP_GAPS.md

# Local deployment compose backups (server-specific, not for publication)
docker-compose.yml.*

# EVAL-001 run artifacts (per-run, regenerated; holdout is aggregate-only)
bench/eval/last-run*.json
