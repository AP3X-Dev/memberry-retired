import type { LabScenarioInput } from '../contracts/scenario.js';
import { LAB_SCENARIO_VERSION } from '../contracts/scenario.js';

const at = (day: number) => `2026-01-${String(day).padStart(2, '0')}T00:00:00.000Z`;

/** Adapter-visible successors to the original MemBench corpus. */
export const RETRIEVAL_INPUTS: readonly LabScenarioInput[] = [
  {
    version: LAB_SCENARIO_VERSION,
    id: 'project-fact-recall-v2',
    split: 'dev',
    title: 'Project fact recall',
    description: 'Repository facts and instructions must be retrieved from realistic distractors.',
    dimensions: ['recall', 'ranking'],
    tenant: 'tenant-alpha',
    project: 'project-recall',
    memories: [
      { id: 'r-pnpm', content: 'This project uses pnpm, not npm, for all package management.', kind: 'fact', recordedAt: at(1) },
      { id: 'r-routes', content: 'API routes live in apps/web/src/app/api.', kind: 'code', recordedAt: at(2) },
      { id: 'r-prisma', content: 'Never edit generated Prisma client files under node_modules/.prisma.', kind: 'instruction', recordedAt: at(3) },
      { id: 'r-service', content: 'Use the existing service layer instead of calling the database directly.', kind: 'instruction', recordedAt: at(4) },
      { id: 'r-docker', content: 'Integration tests require Docker Compose to be running.', kind: 'fact', recordedAt: at(5) },
      { id: 'r-noise1', content: 'The marketing site favicon is a blue hexagon.', kind: 'fact', recordedAt: at(6) },
      { id: 'r-noise2', content: 'The office wifi password rotates monthly.', kind: 'fact', recordedAt: at(7) },
    ],
    queries: [
      { id: 'r-package-manager', query: 'what package manager does this project use', limit: 3 },
      { id: 'r-api-routes', query: 'where do the api routes live', limit: 3 },
      { id: 'r-integration', query: 'how do I run the integration tests', limit: 3 },
    ],
  },
  {
    version: LAB_SCENARIO_VERSION,
    id: 'precision-under-distractors-v2',
    split: 'holdout',
    title: 'Precision under distractors',
    description: 'Authentication evidence must dominate unrelated operational context.',
    dimensions: ['precision', 'ranking'],
    tenant: 'tenant-alpha',
    project: 'project-precision',
    memories: [
      { id: 'p-auth', content: 'Authentication uses JWT bearer tokens validated on every request.', recordedAt: at(1) },
      { id: 'p-login', content: 'The login endpoint issues a signed JWT and a refresh token.', recordedAt: at(2) },
      { id: 'p-d1', content: 'The payment service retries charges with exponential backoff.', recordedAt: at(3) },
      { id: 'p-d2', content: 'Rate limiting uses a token-bucket throttle per client id.', recordedAt: at(4) },
      { id: 'p-d3', content: 'Logs are structured JSON with a correlation id.', recordedAt: at(5) },
      { id: 'p-d4', content: 'The cache layer uses Redis with a 60s TTL.', recordedAt: at(6) },
      { id: 'p-d5', content: 'Deployments run via the pnpm deploy pipeline.', recordedAt: at(7) },
    ],
    queries: [{ id: 'p-auth-query', query: 'how does authentication work', limit: 2 }],
  },
  {
    version: LAB_SCENARIO_VERSION,
    id: 'conflict-resolution-v2',
    split: 'dev',
    title: 'Explicit conflict resolution',
    description: 'Current repository truth must replace explicitly invalidated guidance.',
    dimensions: ['temporal', 'stale-safety', 'ranking'],
    requiredCapabilities: ['temporal-filtering'],
    tenant: 'tenant-alpha',
    project: 'project-conflict',
    memories: [
      { id: 'c-jest-old', content: 'The project uses Jest as its test runner.', kind: 'decision', recordedAt: at(1), invalidatedAt: at(2) },
      { id: 'c-vitest', content: 'The project migrated to Vitest; all tests run with Vitest.', kind: 'decision', recordedAt: at(2), confidence: 0.9 },
      { id: 'c-npm-old', content: 'Deploys run via npm run ship.', kind: 'decision', recordedAt: at(3), invalidatedAt: at(4) },
      { id: 'c-pnpm', content: 'Deploys now run via the pnpm deploy pipeline with rolling restarts.', kind: 'decision', recordedAt: at(4), confidence: 0.9 },
    ],
    queries: [
      { id: 'c-test-runner', query: 'what test runner does the project use', limit: 3 },
      { id: 'c-deploy', query: 'how do we deploy to production', limit: 3 },
    ],
  },
  {
    version: LAB_SCENARIO_VERSION,
    id: 'stale-resistance-v2',
    split: 'holdout',
    title: 'Stale resistance',
    description: 'Several invalidated facts coexist with current truth without polluting context.',
    dimensions: ['temporal', 'stale-safety', 'precision'],
    requiredCapabilities: ['temporal-filtering'],
    tenant: 'tenant-alpha',
    project: 'project-stale',
    memories: [
      { id: 's-cur-db', content: 'The database is PostgreSQL 16 on the primary cluster.', recordedAt: at(5), confidence: 0.9 },
      { id: 's-old-db', content: 'The database was MySQL on a single node.', recordedAt: at(1), invalidatedAt: at(5) },
      { id: 's-old-db2', content: 'We used SQLite for the prototype database.', recordedAt: at(2), invalidatedAt: at(5) },
      { id: 's-cur-queue', content: 'Async work runs on a Redis-backed queue.', recordedAt: at(6), confidence: 0.85 },
      { id: 's-old-queue', content: 'Async work used to run on RabbitMQ.', recordedAt: at(3), invalidatedAt: at(6) },
    ],
    queries: [
      { id: 's-database', query: 'what database does the project use', limit: 3 },
      { id: 's-queue', query: 'how is async work processed', limit: 2 },
    ],
  },
  {
    version: LAB_SCENARIO_VERSION,
    id: 'knowledge-update-v2',
    split: 'holdout',
    title: 'Knowledge update',
    description: 'Valid-time ranges make current and historical truth explicit without adapter-side inference.',
    dimensions: ['temporal', 'stale-safety', 'ranking'],
    requiredCapabilities: ['temporal-filtering'],
    tenant: 'tenant-alpha',
    project: 'project-update',
    memories: [
      { id: 'i-rl-old', content: 'The API rate limit is 100 requests per minute per client.', recordedAt: at(1), validTo: at(3) },
      { id: 'i-noise-a', content: 'The API returns JSON with a correlation id header.', recordedAt: at(2) },
      { id: 'i-rl-new', content: 'The API rate limit is now 1000 requests per minute per client after the upgrade.', recordedAt: at(3), validFrom: at(3) },
      { id: 'i-node-old', content: 'The service runs on Node 18.', recordedAt: at(4), validTo: at(6) },
      { id: 'i-node-new', content: 'The service was upgraded and now runs on Node 22.', recordedAt: at(6), validFrom: at(6) },
    ],
    queries: [
      { id: 'i-rate', query: 'what is the api rate limit', limit: 2 },
      { id: 'i-node', query: 'what node version does the service run on', limit: 2 },
    ],
  },
  {
    version: LAB_SCENARIO_VERSION,
    id: 'multi-hop-recall-v2',
    split: 'dev',
    title: 'Multi-hop recall',
    description: 'A complete answer needs two independently stored facts.',
    dimensions: ['recall', 'ranking'],
    tenant: 'tenant-alpha',
    project: 'project-multihop',
    memories: [
      { id: 'm-token', content: 'Auth tokens are stored in an httpOnly cookie named sid.', recordedAt: at(1) },
      { id: 'm-expiry', content: 'Session tokens expire after 30 minutes of inactivity.', recordedAt: at(2) },
      { id: 'm-d1', content: 'The frontend is built with React and Vite.', recordedAt: at(3) },
      { id: 'm-d2', content: 'Images are served from a CDN.', recordedAt: at(4) },
      { id: 'm-d3', content: 'The CI runs on GitHub Actions.', recordedAt: at(5) },
    ],
    queries: [{ id: 'm-session', query: 'how are sessions stored and when do they expire', limit: 2 }],
  },
  {
    version: LAB_SCENARIO_VERSION,
    id: 'cross-project-isolation-v2',
    split: 'holdout',
    title: 'Cross-project isolation',
    description: 'Lexically similar memories from another project are forbidden.',
    dimensions: ['project-isolation', 'recall'],
    requiredCapabilities: ['project-scope'],
    tenant: 'tenant-alpha',
    project: 'api',
    memories: [
      { id: 'x-web-auth', content: 'Auth uses NextAuth with session cookies.', project: 'web', recordedAt: at(1) },
      { id: 'x-api-auth', content: 'Auth uses JWT bearer tokens verified by middleware.', project: 'api', recordedAt: at(2) },
      { id: 'x-web-db', content: 'The web app reads from a read-replica via Prisma.', project: 'web', recordedAt: at(3) },
      { id: 'x-api-db', content: 'The api writes to the primary Postgres via the repository layer.', project: 'api', recordedAt: at(4) },
      { id: 'x-web-deploy', content: 'The web app deploys to Vercel.', project: 'web', recordedAt: at(5) },
      { id: 'x-api-deploy', content: 'The api deploys as a systemd unit on the cluster.', project: 'api', recordedAt: at(6) },
    ],
    queries: [
      { id: 'x-api-auth-query', query: 'how does auth work', limit: 2 },
      { id: 'x-api-deploy-query', query: 'how does deployment work', limit: 2 },
    ],
  },
] as const;
